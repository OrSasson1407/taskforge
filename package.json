{
  "name": "taskforge-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^4.1.11",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.2"
  },
  "dependencies": {
    "vite": "8.2.2"
  }
}